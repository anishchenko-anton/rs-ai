# .gitignore

# ------------------------------------
# Python-специфичные файлы
# ------------------------------------
# Виртуальное окружение
venv/
.venv/
env/

# Кэш-файлы Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/

# Журналы и временные файлы
*.log
*.tmp
*.bak
*.swp # Vim swap files
*~    # Emacs backup files

# Jupyter Notebook files
.ipynb_checkpoints

# ------------------------------------
# Модели и индексы для RAG-системы
# ------------------------------------
# Локально скачанные модели Sentence Transformers
# Hugging Face по умолчанию кэширует модели здесь:
# Windows: C:\Users\ВАШ_ЮЗЕР\.cache\huggingface\hub
# Linux/macOS: ~/.cache/huggingface/hub
# Или старый путь: ~/.cache/torch/sentence_transformers
# Мы не будем явно игнорировать этот путь, так как он находится вне папки проекта.
# Но если вы будете сохранять модели напрямую в проекте, добавьте путь сюда:
fine_tuned_model/ # Если вы сохраняете дообученную модель в эту папку

# FAISS индексы
faiss_indexes/ # Игнорируем всю директорию, где хранятся индексы FAISS

# ------------------------------------
# Данные
# ------------------------------------
# Файлы с базой знаний, которые генерируются/изменяются скриптом
# Если knowledge_base.json создается динамически или содержит чувствительные данные,
# можно игнорировать его. Если он является частью исходных данных, то нет.
# В нашем случае он генерируется, так что его можно игнорировать.
rag_system/data/knowledge_base.json

# ------------------------------------
# Разное
# ------------------------------------
.DS_Store # macOS
Thumbs.db # Windows